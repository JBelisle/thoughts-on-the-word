---
const path = Astro.url.pathname;
const isActive = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
const links = [
  { href: '/', label: 'Home' },
  { href: '/reading-plans', label: 'Reading Plans' },
  { href: '/theology', label: 'Theology Essays' },
  { href: '/browse', label: 'All Content' },
  { href: '/discussion-starters', label: 'Discussion Starters' },
  { href: '/about', label: 'About' },
];
---

<!-- Two siblings (not wrapped in one element) so .nav-bar can stick to the page, not the header. -->
<header class="brand-bar">
  <div class="container brand-row">
    <a class="brand" href="/">
      <img class="brand-logo" src="/images/totw-logo.png" alt="" width="28" height="28" />
      <span>Thoughts on the Word</span>
    </a>
    <div class="actions">
      <a class="icon-btn" href="/search" aria-label="Search"><i class="ti ti-search" aria-hidden="true"></i></a>
      <button class="icon-btn" type="button" aria-label="Toggle light or dark theme" onclick="__toggleTheme()">
        <i class="ti ti-sun" data-theme-icon aria-hidden="true"></i>
      </button>
    </div>
  </div>
</header>
<nav class="nav-bar" aria-label="Primary">
  <div class="container nav-inner">
    {links.map((l) => (
      <a href={l.href} aria-current={isActive(l.href) ? 'page' : undefined} style={isActive(l.href) ? 'color: var(--text); font-weight: 600' : undefined}>{l.label}</a>
    ))}
  </div>
</nav>

<script is:inline>
  function __toggleTheme() {
    var el = document.documentElement;
    var next = el.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
    el.setAttribute('data-theme', next);
    try { localStorage.setItem('theme', next); } catch (e) {}
    document.querySelectorAll('[data-theme-icon]').forEach(function (i) {
      i.className = 'ti ' + (next === 'dark' ? 'ti-sun' : 'ti-moon');
    });
  }
  (function () {
    var d = document.documentElement.getAttribute('data-theme');
    document.querySelectorAll('[data-theme-icon]').forEach(function (i) {
      i.className = 'ti ' + (d === 'dark' ? 'ti-sun' : 'ti-moon');
    });
  })();
</script>
